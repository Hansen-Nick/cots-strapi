'use strict';

/**
 * authentication-password router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::authentication-password.authentication-password');
